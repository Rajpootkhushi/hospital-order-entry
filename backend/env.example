# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration - MongoDB Atlas (Recommended)
# Replace <username>, <password>, and <cluster-url> with your actual values
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/patient-reception?retryWrites=true&w=majority

# Alternative: Local MongoDB (if you have MongoDB installed locally)
# MONGODB_URI=mongodb://localhost:27017/patient-reception

# Frontend URL
FRONTEND_URL=http://localhost:3000

# JWT Secret (for authentication)
JWT_SECRET=your_jwt_secret_key_here

# Optional: MongoDB Atlas URI (for production)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/patient-reception 